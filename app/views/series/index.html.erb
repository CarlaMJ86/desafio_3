<h1>Series#index</h1>
<p>Find me in app/views/series/index.html.erb</p>

<%= link_to 'Ingresar una nueva Serie', series_new_path %>

<div>
    <table class="table table-dark">
        <thead>
            <tr>
                <th scope="col">N°</th>
                <th scope="col">Nombre de la Serie</th>
                <th scope="col">Sinopsis</th>
                <th scope="col">Director</th>
            </tr>
        </thead>
        <tbody>
            <% @series.each do |serie| %>
            <tr>
                <th scope="row"><%= serie.id %></th>
                <td><%= serie.name %></>
                <td><%= serie.synopsis %></td>
                <td><%= serie.director %></td>
            </tr>
        <% end %>
        </tbody>
    </table>
</div>